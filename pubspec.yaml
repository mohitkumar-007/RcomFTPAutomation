name: rcom_ftp_automation
description: QA workspace — specs, reporting tools and CI config for the Rummy gameplay automation.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.6.0
